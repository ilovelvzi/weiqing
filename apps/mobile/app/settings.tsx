import { Text } from "react-native";
import { PageShell } from "../src/components/layout";

export default function SettingsRoute() {
  return (
    <PageShell title="Settings" description="Preferences placeholder.">
      <Text>Reminder, unit, privacy, and theme settings will be wired later.</Text>
    </PageShell>
  );
}
