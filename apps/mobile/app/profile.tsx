import { Text } from "react-native";
import { PageShell } from "../src/components/layout";

export default function ProfileRoute() {
  return (
    <PageShell title="Profile" description="User profile placeholder.">
      <Text>Profile summary and edit entry points will be added later.</Text>
    </PageShell>
  );
}
