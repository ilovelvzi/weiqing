import { Text } from "react-native";
import { PageShell } from "../src/components/layout";

export default function CheckInRoute() {
  return (
    <PageShell title="Check in" description="Weight entry placeholder.">
      <Text>Weight input, mood, note, and success state are reserved for later.</Text>
    </PageShell>
  );
}
