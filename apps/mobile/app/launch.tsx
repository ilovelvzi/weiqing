import { Text } from "react-native";
import { PageShell } from "../src/components/layout";

export default function LaunchRoute() {
  return (
    <PageShell title="Launch" description="Startup route placeholder.">
      <Text>Token check and routing will be wired in the next mobile phase.</Text>
    </PageShell>
  );
}
